P1:
- Fix Jenkinsfile
- Decrease the height & width of the generated png img to include only the actual drawn area.
- Align cropped image to the center so that annotations can be added on the left and above too.

P2
- Capture current tab by default, and provide a popup window to select otherwise.
- Place all configuration options is a separate 'config' file.
- Auto-scroll when mouse is out of canvas ('onmouseout'); this will allow capturing more than is visible.

P3
- Button to copy image instead of URL.
- Undo button.
- Line thickness.
- Line color.
