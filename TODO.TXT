P1:
- Fix Jenkinsfile

P2
- Auto-scroll when mouse is out of canvas ('onmouseout'); this will allow capturing more than is visible.

P3
- Button to copy image instead of URL.
- Undo button.
- Line thickness.
- Line color.
- Currently it is difficult to crop from the top-left side of the image (give it some margin).
